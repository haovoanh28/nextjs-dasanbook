import {Box} from "@mui/material";

import SubmissionPageHeader from "@/ui/submission/PageHeader";
import SubmissionContainer from "@/ui/submission/Container";

const Page = () => {
  return (
    <Box>
      <SubmissionPageHeader/>
      <SubmissionContainer/>
    </Box>
  );
}

export default Page;